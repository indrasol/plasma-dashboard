# name: Deploy to Cloud Run

# on:
#   push:
#     branches:
#       - main
#       - stage
#       - dev
#     paths:
#       - 'server_fastapi/**'
#       - 'Dockerfile'
#       - 'requirements.txt'
#       - '.github/workflows/deploy-cloudrun.yml'

# env:
#   PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
#   REGION: us-central1
#   REPO_NAME: plasma-repo
#   # Dynamic Service Name based on branch (default to dev)
#   SERVICE_NAME: plasma-backend-${{ github.ref_name == 'main' && 'prod' || github.ref_name == 'stage' && 'stage' || 'dev' }}

# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     environment: ${{ github.ref_name }} # Use GitHub Environments (dev, stage, prod)

#     permissions:
#       contents: 'read'
#       id-token: 'write'

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Google Auth
#         id: auth
#         uses: 'google-github-actions/auth@v2'
#         with:
#           credentials_json: '${{ secrets.GCP_SA_KEY }}'

#       - name: Set up Cloud SDK
#         uses: 'google-github-actions/setup-gcloud@v2'

#       - name: Configure Docker
#         run: |
#           gcloud auth configure-docker ${{ env.REGION }}-docker.pkg.dev

#       - name: Build Docker Image
#         run: |
#           # Tag with branch name and commit sha
#           IMAGE_TAG=${{ env.REGION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.REPO_NAME }}/${{ env.SERVICE_NAME }}:${{ github.sha }}
#           docker build -t $IMAGE_TAG .
#           echo "IMAGE_TAG=$IMAGE_TAG" >> $GITHUB_ENV

#       - name: Push Docker Image
#         run: |
#           docker push ${{ env.IMAGE_TAG }}

#       - name: Deploy to Cloud Run
#         uses: 'google-github-actions/deploy-cloudrun@v2'
#         with:
#           service: ${{ env.SERVICE_NAME }}
#           region: ${{ env.REGION }}
#           image: ${{ env.IMAGE_TAG }}
#           # Environment variables injected from GitHub Secrets based on the environment context
#           env_vars: |
#             SUPABASE_URL=${{ secrets.SUPABASE_URL }}
#             SUPABASE_SERVICE_ROLE_KEY=${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
#             SUPABASE_ANON_KEY=${{ secrets.SUPABASE_ANON_KEY }}
#             ENV=${{ github.ref_name }}
